package user

type User struct {
	Name string
}

func (u *User) Save() *User {
	// normal comment
	fmt.Println("start saving user")
	metrics.Track("start saving user")
	Db.save(u)
	fmt.Println("end saving user")
	metrics.Track("end saving user")
}

